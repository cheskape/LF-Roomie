<%= link_to 'Home', root_path %> 

<br>
<h2>Matches</h2>
<table>
  <thead>
    <tr>
      <th>...</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @matches.each do |match| %>
      <tr>

        <td><%= User.find(match.id).getInfo.firstname %></td>

      </tr>
    <% end %>
  </tbody>
</table>
